# Usado por default.conf: preserva X-Forwarded-Proto do Traefik/Dokploy quando existir,
# senao cai no $scheme (acesso direto ao container).
map $http_x_forwarded_proto $motasa_forwarded_proto {
    default $http_x_forwarded_proto;
    ""      $scheme;
}
